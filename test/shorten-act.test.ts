import { expect } from 'chai';
import { ShortenAct } from '../src';

describe('ShortenAct', function () {
    it('no services should be present', function () {
        expect(5).to.equal(5);
    });
});